# 7. Conclusion

The Leibniz series is discoverable by genetic programming from arithmetic primitives and a log-precision fitness function. The fitness need not mention π, alternating signs, or odd denominators: these emerge as the simplest structure satisfying sustained constant-rate precision gain. Discovery at population 1,000 on the minimal 4-terminal set succeeded in all {{result:entropy_minimal_5_5:value}}/5 trials within {{result:entropy_minimal_runtime:value}} seconds total.

Discovery is reliable under constrained search spaces and fails sharply as the search space expands. The scaling grid (t = 4 through t = 20, populations 1,000 through 10,000) places the phase transition boundary between t=8 and t=10 for all tested populations. At t=4, success is reliable. At t=10 and above, no population size up to 10,000 achieves consistent discovery. The anomalous partial recovery at t=15 with pop=10,000 (2/5 seeds) does not contradict this pattern. The anomaly suggests that sufficiently large populations can occasionally maintain the coverage needed to escape wrong-limit attractor basins, but cannot do so reliably.

The failure is not in the fitness landscape. Leibniz sits at or near the global optimum of the log-precision fitness in all tested configurations. The fitness correctly ranks Leibniz above every wrong-limit attractor when both are present. The question is harder than the answer: whether the search can cover enough of the space for correct building blocks to appear before wrong-limit attractors occupy and hold the population.

This framing, that discovery is governed by the ratio of coverage to search space rather than by fitness quality, has consequences for symbolic regression on infinite-horizon problems generally. Richer terminal sets, larger search spaces, and more expressive operators all increase the density of exploitable wrong-limit attractors faster than any population increase can compensate. Fitness engineering alone cannot solve the coverage problem.

The core finding suggests three directions for future work. First, building block initialization: instead of random trees, a pre-pass over the terminal set could identify structurally promising subtrees (oscillating signs, growing denominators) and seed the population with their recombinations. Seeding subtrees is not injecting the answer; it is injecting vocabulary. Second, automated terminal set pruning: a short GP pre-pass could identify which terminals appear in high-fitness expressions, then prune the rest before the full search. This automates the constraint that our experiments show matters most. Third, island migration models: running multiple independent small populations with periodic migration of top individuals could allow successful seeds to share building blocks with unsuccessful ones, addressing the coverage bottleneck directly. Each of these directions attacks the actual bottleneck identified by our experiments: not the fitness landscape, but the initial coverage of correct building blocks in an expanding search space.
